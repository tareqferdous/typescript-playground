// Conditional Type in TypeScript
// =============================================

/* 
Conditional Type কী?

Conditional Type হচ্ছে TypeScript-এর এমন এক বৈশিষ্ট্য,
যার মাধ্যমে আমরা টাইপের ভিতরে condition (শর্ত) বসাতে পারি।

মানে:
👉 “যদি একটি টাইপ অন্য টাইপের সাথে মিলে যায় তাহলে একরকম টাইপ দাও,
   না হলে অন্যরকম টাইপ দাও।”

এটা অনেকটা if-else statement-এর মতো, কিন্তু TypeScript type system-এর ভেতরে কাজ করে।
*/

// ======================================================
//  ১. সাধারণ উদাহরণ (Basic Conditional Type)
// ======================================================

type A = null;
type B = undefined;

// নিচের লজিকটা একটু খেয়াল করি 👇
// ==>: যদি A null এর সমান হয় ➜ true
// ==>: না হলে যদি B undefined হয় ➜ true
// ==>: না হলে ➜ false

type C = A extends null ? true : B extends undefined ? true : false;

// ✅ এখানে C = true (কারণ A = null)

// =============================
//  ব্যাখ্যা:
// =============================
// `extends` এখানে তুলনার কাজ করে।
// মানে আমরা বলছি: “A কি null টাইপকে extend করে?”
// যদি করে, তাহলে true না করলে অন্য টাইপ দাও।

// ======================================================
//  ২️. ব্যবহারিক উদাহরণ (Practical Example)
// ======================================================

/*
ধরো আমাদের একটা object আছে যেখানে বিভিন্ন যানবাহনের property আছে।
আমরা এখন একটা Conditional Type বানাবো,
যেটা দিয়ে যাচাই করা যাবে কোনো key ওই object-এর মধ্যে আছে কি না।
*/

type RecePipleVhicle = {
  bike: string;
  car: string;
  ship: string;
};

// ✅ এখন আমরা একটি Conditional Type বানাবো:
type CheckVehicle<T> = T extends keyof RecePipleVhicle ? true : false;

/*
এখানে যা হচ্ছে:
-----------------------------
- keyof RecePipleVhicle ➜ "bike" | "car" | "ship"
- T যদি এর মধ্যে পড়ে ➜ true
- না হলে ➜ false
*/

// ======================================================
//  ৩️. ফলাফল (Test Cases)
// ======================================================

type HasBike = CheckVehicle<"bike">; // ✅ true
type HasPlane = CheckVehicle<"plane">; // ❌ false
type HasCar = CheckVehicle<"car">; // ✅ true

/*
 ব্যাখ্যা:
--------------------------------
যখন আমরা CheckVehicle<"bike"> দেই:
→ "bike" কি keyof RecePipleVhicle এর মধ্যে আছে? ✅ হ্যাঁ
তাই result হবে true।

কিন্তু CheckVehicle<"plane"> দিলে:
→ "plane" কি ওই object এর key? ❌ না
তাই result হবে false।
*/

// ======================================================
//  সংক্ষেপে:
// ======================================================
//
//  Conditional Type = “type-এর ভিতরে if-else।”
//
// উদাহরণ:
// T extends U ? X : Y
//
// ==>: যদি T, U-এর সাথে মিলে যায় ➜ X টাইপ দাও
// ==>: না হলে ➜ Y টাইপ দাও
//
// ✅ বাস্তবে:
// - API response অনুযায়ী টাইপ পরিবর্তন
// - Utility type (Extract, Exclude, NonNullable ইত্যাদি)
// - Dynamic generic constraint তৈরিতে ব্যবহৃত হয়
//
