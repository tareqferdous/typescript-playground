// =========================================================
// 🔵 Getter & Setter কী?
// =========================================================
/*
Getter এবং Setter হলো class-এর ভিতরে থাকা বিশেষ method,
যেগুলো দিয়ে আমরা কোনো private property কে পড়তে (get) বা লিখতে (set) পারি।

🔹 Getter → কেবল ডেটা পড়ার জন্য  
🔹 Setter → ডেটা সেট/আপডেট করার জন্য  

👉 কেন দরকার?
----------------
✔ private property বাইরে থেকে direct অ্যাক্সেস করা যায় না  
✔ কিন্তু অনেক সময় আমরা ওটাকে পড়তে বা আপডেট করতে চাই  
✔ তাই Getter & Setter ব্যবহার করলে:
   - ডেটা নিয়ন্ত্রণ করা যায় (validation করা যায়)
   - property নিয়ন্ত্রণ করে নিরাপদভাবে access দেওয়া যায়
*/

// =========================================================
// 🧩 Example 1: Name property নিয়ে Getter & Setter
// =========================================================

class User {
  private _name: string; // private মানে এটি class-এর বাইরে থেকে সরাসরি অ্যাক্সেস করা যাবে না

  constructor(name: string) {
    this._name = name;
  }

  // -----------------------
  // 🔹 Getter (Only Read)
  // -----------------------
  get name() {
    // যেই মানই আসুক, uppercase করে return করা হবে
    return this._name.toUpperCase();
  }

  // -----------------------
  // 🔹 Setter (Write / Update)
  // -----------------------
  set name(value: string) {
    // এখানে আমরা validation করতে পারি
    if (value.length === 0) {
      throw new Error("Name cannot be empty!");
    }
    this._name = value;
  }
}

// ⭐ ব্যবহার
const user = new User("Anwarul");

console.log(user.name); // Getter → আউটপুট: ANWARUL

user.name = "Karim"; // Setter → নতুন মান সেট করল
console.log(user.name); // আউটপুট: KARIM

// =========================================================
// 🧩 Example 2: Age property নিয়ে Getter & Setter
// =========================================================

class Person {
  private _age: number = 0; // default age = 0

  // Getter → age read করার জন্য
  get age() {
    return this._age;
  }

  // Setter → age update করার জন্য
  set age(value: number) {
    if (value < 0) {
      // validation
      throw new Error("Age cannot be negative");
    }
    this._age = value;
  }
}

// ⭐ ব্যবহার
const p = new Person();

p.age = 20; // Setter call
console.log(p.age); // Getter call → Output: 20

p.age = -5; // ❌ Error: Age cannot be negative
