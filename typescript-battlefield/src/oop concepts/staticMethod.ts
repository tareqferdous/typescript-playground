/* 
===========================================
 1️⃣ Static কী? (Concept + Memory Behavior)
===========================================

✔ Static মানে:
-------------------------------------------
👉 class লোড হওয়ার সাথে সাথে property/method তৈরি হয়  
👉 object তৈরি হোক বা না হোক — static জিনিসগুলো সবসময় ক্লাস থেকেই অ্যাক্সেস করতে হয়

🔹 Instance Property/Method:
    - Object তৈরি হলে memory পায়  
    - প্রতিটি object তার নিজস্ব copy পায়

🔹 Static Property/Method:
    - Object ছাড়াই class-এর ভেতরেই থাকে
    - পুরো class-এর জন্য মাত্র একটাই copy হয়
    - সব object মিলেও একই static property share করে

🔹 Static কখন কাজের?  
    - Count করা
    - Global config রাখা
    - Utility ফাংশন তৈরি করা
    - Object কতগুলো তৈরি হলো হিসাব রাখা

-------------------------------------------
*/

class Counter {
  // 🔥 Static property → class load হওয়ার সাথে সাথে তৈরি হবে
  static count: number = 0;

  constructor() {
    // 🔥 প্রতিবার object তৈরি হলে static count বাড়বে
    Counter.count++;
  }

  // 🔥 Static method → object দিয়ে নয়, class দিয়ে কল হবে
  static getCount() {
    return `Total objects created: ${Counter.count}`;
  }
}

// -------------------------------
// 🎯 Object তৈরি করি
// -------------------------------
const c1 = new Counter(); // count = 1
const c2 = new Counter(); // count = 2
const c3 = new Counter(); // count = 3

// -------------------------------
// 🎯 Static method কল (class দিয়ে)
// -------------------------------
console.log(Counter.getCount()); // Total objects created: 3
